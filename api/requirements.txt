# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-multipart>=0.0.20
loguru>=0.7.3

# LLM / HTTP
openai>=1.59.0
httpx>=0.28.0

# RAG stores
qdrant-client>=1.12.0
neo4j>=5.27.0

# Observability
langfuse>=2.59.0

# State
redis[hiredis]>=5.2.0

# Utils
python-dotenv>=1.0.0

# ── V1 parsing (kept for backward compat) ───────────────────────────────
docling>=2.0.0

# ── Pipeline V2 — Multi-format parsing ──────────────────────────────────
openpyxl>=3.1.0
python-docx>=1.1.0
mammoth>=1.8.0
pypdf>=5.1.0

# ── Pipeline V2 — Cross-encoder reranking (Stage 1) ─────────────────────
# Heavy (~1.5 GB with torch). Optional — code has graceful fallback via
# rerank_stage2 if missing. Comment out to speed up build.
sentence-transformers>=3.0.0
torch>=2.2.0

# ── Pipeline V2 — GraphRAG community detection ──────────────────────────
# Code has Louvain fallback (pure networkx) if igraph fails to install.
networkx>=3.0
python-igraph>=0.11.0
leidenalg>=0.10.0
